# Optional Testing Requirements
# These enable full functionality but tests will run in mock mode without them
# Install with: pip install -r test-requirements-optional.txt

# Vector Store backends (for mcp-vectorstore)
chromadb>=0.4.0
faiss-cpu>=1.7.0

# Embedding providers (for mcp-embeddings)
openai>=1.0.0
sentence-transformers>=2.2.0
torch>=2.0.0

# Data source parsers (for mcp-datasources)
beautifulsoup4>=4.11.0
PyPDF2>=3.0.0
pdfplumber>=0.9.0
python-docx>=0.8.0
pandas>=2.0.0
sqlalchemy>=2.0.0

# NLP tools (for advanced chunking)
spacy>=3.5.0
nltk>=3.8.0

# HTTP clients (for retriever/datasources)
requests>=2.28.0
httpx>=0.24.0

# LLM providers (for generator/reranker)
anthropic>=0.18.0
